# Placeholder Powerlevel10k config. Run `p10k configure` in an interactive
# shell to regenerate this file with a real prompt style (it overwrites this
# path directly). Once you're happy with the result, copy the regenerated
# ~/.p10k.zsh back into dotfiles/zsh/.p10k.zsh and commit it if you want the
# tuned prompt to follow you across machines.
